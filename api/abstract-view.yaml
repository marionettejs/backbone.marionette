name: AbstractView

class: Marionette.AbstractView

description:

examples:

properties:

constructor: |
  @param {Object} options

functions:
  
  getTemplate: |
    @api public
    
  _renderTemplate: |
    @api private

  serializeModel: |
    @api public
    @param {} model
    
  mixinTemplateContext: |
    @api public
    @param {} target
    
  normalizeUIKeys: |
    @api public
    @param {Object} hash
    
  configureTriggers: |
    @api public
    
  delegateEvents: |
    @api public
    @param {Object} events
  
  _delegateDOMEvents: |
    @api private
    @param {Object} events
  
  undelegateEvents: |
    @api public
  
  onShowCalled: |
    @api public
  
  _ensureViewIsIntact: |
    @api private
  
  destroy: |
    @api public
    @returns {Marionette.View} The current view.
  
  bindUIElements: |
    @api public
  
  unbindUIElements: |
    @api public

  getUI: |
    @api public
    @param {String} name - the name of the ui element to receive
    @returns {jQuery} - the jQuery object of the ui element

    description: |
      A stable interface to the view's ui hash. 
      Will return the specified ui element if it is available. If the view is destroyed
      a `ViewDestroyedError` will be thrown.
